#include <stdio.h>
#include <ctype.h>

/**
* main - Entry point
*
* Return: Always 0 (success)
*/

int main(void)
{
char ch = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
lower = tolower(ch);
putchar(ch);

return (0);
}
