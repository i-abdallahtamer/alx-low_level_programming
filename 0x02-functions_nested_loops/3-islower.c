#include "main.h"
/**
* print_alphabet - Entry point
* Description: prints the alphabet in small caps
* Return: Always 0 (Success)
*/

int _islower(int c)
{

if (c == 'c')
{
return (1);
}
else
{
return (0);
}
}
