#include "main.h"
/**
 *  * main - Entry point
 *   *
 *    * Return: Always 0 (Success)
 *     */
int main(void)
char ch;
char space='\n';
{
for (ch ="a" ch <= z; ch++)
{
_putchar(tolower(ch));
}
_putchar(space);

