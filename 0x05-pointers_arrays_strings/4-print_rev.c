#include "main.h"

/**
 * rev_string - return the length of a string
 * @s : Parameter accept  string
 * Return: nothng
 *
 */

void rev_string(char *s)
{
int c;
int len;
for (c = 0; str[c] != '\0'; c++)
{
}
len = c;

for (i = len; i >= 0; i--)
{
_putchar(s[i]);
}
_putchar('\n');
}
