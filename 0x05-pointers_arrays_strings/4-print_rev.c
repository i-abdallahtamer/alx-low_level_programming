#include "main.h"

/**
 * print_rev - return the length of a string
 * @s : Parameter accept  string
 * Return: nothng
 *
 */

void print_rev(char *s)
{
int c;
int len;
int i;
char space = '\n';
for (c = 0; s[c] != '\0'; c++)
{
}
break;
len = c;
while (len >= 0)

{
_putchar(s[i]);
len--;
}
_putchar(space);
}
