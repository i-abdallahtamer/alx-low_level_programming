#include "main.h"

/**
 * rev_string - return the length of a string
 * @s : Parameter accept  string
 * Return: nothng
 *
 */

void rev_string(char *s)
{
int c;
int len;
int i = 0;

for (c = 0; s[c] != '\0'; c++)
{
}
len = c;

while (i < len)
{
_putchar(s[len]);
len--;
}

}
