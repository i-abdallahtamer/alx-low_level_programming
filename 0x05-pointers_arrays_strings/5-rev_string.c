#include "main.h"

/**
 * rev_string - return the length of a string
 * @s : Parameter accept  string
 * Return: nothng
 *
 */

void rev_string(char *s)
{
int c;
int len;
int i;
char q, p;

for (c = 0; s[c] != '\0'; c++)
{
s[c] = p;
}
len = c;


for (i = len; i >= 0; i--)
{
s[i] = q;
if (i == 0)
{
break;
}

}

}
